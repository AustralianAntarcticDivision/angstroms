#' Create SpatialPoints (deprecated).
#' 
#' This function is kept for backward compatibility. See [coords_points()] which now returns
#' a plain matrix. For the old SpatialPoints return, convert manually.
#' @name coupling-deprecated
NULL
